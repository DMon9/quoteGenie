name = "estimategenie"
compatibility_date = "2024-01-01"

# Pages build output directory (required for wrangler.toml support)
pages_build_output_dir = "."

# Custom headers and redirects are handled by _headers and _redirects files
